<<<TAR_UNCOMPRESS>>>
    <<<PATCH>>>
    mkdir cross-build
    pushd cross-build
        ${CROSSTOOLS_DIR}/bin/python3 \
        ../configure.py --platform=linux-loongarch64-gnu-g++ \
                     --sysroot=${SYSROOT_DIR}/usr
	<<<MAKE>>>
        make install
    popd
<<<CLEAR_DIR>>>
