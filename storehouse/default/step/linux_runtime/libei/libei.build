<<<TAR_UNCOMPRESS>>>
        <<<PATCH>>>
#	sed -i "1s@ python3@ ${LINUX_HOST}-python3@g" proto/ei-scanner
        mkdir cross-build
        pushd cross-build
                meson ${MESON_CONFIG} --default-library=both -Dtests=disabled \
                      ..
                <<<NINJA_AND_INSTALL>>>
        popd
        rm -f ${LINUX_SYSROOT_DIR}/usr/lib${LIB_SUFF}/*.la
<<<CLEAR_DIR>>>
