<<<TAR_UNCOMPRESS>>>
	<<<PATCH>>>
	mkdir cross-build
	pushd cross-build
		meson ${MESON_CONFIG} --default-library=both <<<EXTRA_PARM>>> \
                      ..
		<<<NINJA_AND_INSTALL>>>
	popd
	rm -f ${X86_SYSROOT_DIR}/usr/lib/*.la
<<<CLEAR_DIR>>>
