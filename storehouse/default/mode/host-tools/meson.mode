<<<TAR_UNCOMPRESS>>>
    <<<PATCH>>>
    mkdir -p native-build
    pushd native-build
        meson --prefix=${PREFIX} --libdir=${PREFIX}/lib${LIB_SUFF} <<<EXTRA_PARM>>> \
              --buildtype=release ..
        ninja
        ninja install
    popd
<<<CLEAR_DIR>>>
