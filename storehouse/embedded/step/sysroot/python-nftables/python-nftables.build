<<<TAR_UNCOMPRESS>>>
	pushd py
		CC=${CROSS_TARGET}-gcc CXX=${CROSS_TARGET}-g++ _PYTHON_SYSCONFIGDATA_NAME=_sysconfigdata__linux_${CROSS_TARGET} <<<CROSS_TOOLS_PREFIX>>>/bin/pip3 wheel -w dist --no-build-isolation --no-deps ${PWD}
		CC=${CROSS_TARGET}-gcc CXX=${CROSS_TARGET}-g++ _PYTHON_SYSCONFIGDATA_NAME=_sysconfigdata__linux_${CROSS_TARGET} <<<CROSS_TOOLS_PREFIX>>>/bin/pip3 install --no-index --find-links dist --no-cache-dir --no-deps --ignore-installed --no-user <<<PACKAGE_NAME>>> --root=${SYSROOT_DIR} --prefix=/usr
	popd
<<<CLEAR_DIR>>>
